spring:
  application:
    name: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8180/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
